# aanp1938 has its own dedicated repository (codestarnischal/aanp1938).
# The local directory exists only as a staging copy — it is not part of
# this repo's deployment and should not be committed here.
aanp1938/
